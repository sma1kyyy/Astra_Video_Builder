# ОСНОВА YAML-СКРИПТА
Принципы написания скрипта, актуальные для любого режима записи.

*Обязательные поля помечены \* перед названием*

# * metadata
> Свойства видео-ролика, такие как название, описание, разрешение или количество кадров в секунду

---
- \* **title** - название видео-ролика
- \* **resolution** - разрешение видео-ролика. Значение задаётся в следующем формате: "ШиринаxВысота"
- **description** - описание видео-ролика для логирования
- **language** - язык восприятия текста и TTS. Доступны: 
    - ru *(default)*
    - en
- **mode** - режим записи видео-ролика. Доступны:
    - live *(default)*
    - screenshot
> Примечание: значение `screenshots` (во множественном числе) в runtime не поддерживается.
- **browser** - браузер для режима *live*. Доступны:
    - chrome *(default)*
    - firefox
- **cursor** - отображать курсор или нет. Доступные значения:
    - true 
    - false *(default)*
- **fps** - количество кадров в секунду. Доступны:
    - 24
    - 30 *(default)*
    - 60
- **save_files** - сохранять ли промежуточные файлы (TTS-аудио, исходную запись и т. д.) после рендера. Доступные значения:
    - true
    - false *(default)*

# acts
> Содержит в себе условные *акты*, из которых состоит видео-ролик. В каждом акте есть определенное количество сцен.

## act_object
> Объект акта, в котором расположены сцены. Название соответствует формату "act_id", где id - уникальный порядковый номер акта.

- **name** - название для акта. По умолчанию стоит id акта.

### * scenes
> Содержит в себе сцены, фрагменты, из которых состоит акт

---
#### scene_object
> Объект сцены, который содержит информацию о фрагменте видео-ролика, такие как длительность, действия, которые в нём происходят, изображения или музыка. Название соответствует формату: "scene_id", где id - уникальный порядковый номер сцены. Видео-ролик формируется из фрагментов по порядку, основанному на id.

---
- **duration** - минимальная длительность фрагмента в секундах (1 по умолчанию).
- **name** - название сцены для логирования
- **tts** - текст, который будет синтезироваться в речь
- **voice** - тип воспроизводимого голоса из tts. Доступные варианты:
    - jane *(default)*
    - zahar
- **subtitles** - включить или выключить субтитры, если работает tts. Доступные варианты:
    - true
    - false *(default)*
- **subplace** - место для субтитров. Работает только при значении *true* у *subtitles*. Доступные варианты:
    - up
    - down *(default)*
    - center
- **effect** - определяет фильтр всего изображения. Доступные варианты:
    - *without* *(default)* - без особого фильтра
    - *black_white* - чёрно-белый фильтр
- **images** - содержит объекты *image_object*, которые будут размещены на фрагменте
- **audio** - содержит объекты *audio_object*, которые будут размещены на фрагменте
- **texts** - содержит объекты *text_object*, которые будут размещены на фрагменте

### image_object
> Определяет изображение, которое будет наложено на сцене. Название формируется по следующему шаблону: "image_id" - где id - уникальный порядковый номер изображения. Все изображения формируются по порядку, основанному на id.

---
- \* **path** - путь до изображения (файлы .png или .jpg)
- \* **start_x** - Задаёт изначальное положение изображения по x.
- \* **start_y** - Задаёт изначальное положение изображения по y.
- \* **resolution** - разрешение изображения, параметр, определяющий конечные точки расположения изображения. Значение задаётся в следующем формате: "ШиринаxВысота"
- **wait** - указывает длительность ожидания наложения изображения в секундах. Если задано, изображения появится только через переданный промежуток времени в рамках очереди, при этом задерживая оставшиеся изображения.
- **duration** - указывает длительность наложения изображения в секундах. Если задано, очередь к следующему изображению перейдет только тогда, когда текущее будет окончено.

### audio_object
> Определяет аудио (файл .mp3), которое будет на сцене. Название формируется по следующему шаблону: "audio_id" - где id - уникальный порядковый номер аудио. Все аудио формируются по порядку, основанному на id.

---
- \* **path** - путь до аудио (файлы .mp3)
- \* **_from** - откуда начать воспроизведение в секундах относительно аудио
- \* **duration** - сколько будет воспроизводится аудио в секундах, начиная с *from*.
- **wait** - сколько ждать перед началом воспроизведения в секундах (0 по умолчанию, то есть без задержки)

### text_object
> Определяет текст для сцены. Название формируется по следующему шаблону: "text_id" - где id - уникальный порядковый номер текста. Все текста формируются по порядку, основанному на id.

---
- \* **text** - задаёт содержание текста
- \* **start_x** - задаёт изначальное положение изображения по x.
- \* **start_y** - задаёт изначальное положение изображения по y.
- \* **size** - задаёт размер текста в пикселях
- **font** - задаёт шрифт текста из доступных. Доступные варианты:
    - *arial* *(default)*
    - *comic_sans*
- **wait** - указывает длительность ожидания наложения текста в секундах. Если задано, текст появится только через переданный промежуток времени в рамках очереди, при этом задерживая оставшиеся объекты.
- **duration** - указывает длительность наложения текста в секундах. Если задано, очередь к следующему тексту перейдет только тогда, когда текущий будет окончен.

# Особенности Live Recording
## metadata
Дополнительное поле, специфичное для Live Recording:
- **browser** — браузер для автоматизации. Доступны:
    - chrome *(default)*
    - firefox

Поле `cursor` тоже актуально, но имеет нюанс: в Live Recording курсор автоматически паркуется в правый нижний угол окна при старте браузера. Это нужно для Wayland-сессий, где штатные средства рекордера не позволяют скрыть курсор. Если вы хотите видеть курсор в видео — добавьте действия `click` / `scrollTo`, и Selenium переместит его к элементам. Если хотите его полное отсутствие — не используйте действия, перемещающие курсор.

## scene_object
> Содержит дополнительное необязательное поле *actions*, в котором размещены *action_object*. Это действия, воспроизводимые в браузере в реальном времени.

Если в сцене указан `tts`, длительность аудио учитывается: если действия выполнились быстрее озвучки, сцена дотягивает `sleep` до её окончания.

#### action_object
> Определяет действие, которое необходимо совершить в реальном времени. Название формируется по следующему шаблону: "action_id" - где id - уникальный порядковый номер действия. Все действия сцены формируются по порядку, основанному на id.

---
- \* **type** - тип выполняемого действия. Регистр scroll-типов не важен (`scrollUp` = `scrollup`). Доступные типы:
    - *navigate* - перейти по какому-то URL
    - *click* - нажать на какой-то элемент DOM. При отсутствии элемента — `Element not found`, сцена прерывается.
    - *wait* - бездействовать в течение какого-то времени
    - *input* - ввести текст в форму. По умолчанию после ввода автоматически нажимается ENTER (см. поле `enter`).
    - *scrollUp* / *scrollup* - поднять страницу вверх
    - *scrollDown* / *scrolldown* - опустить страницу вниз
    - *scrollTo* / *scrollto* - прокрутить к элементу (он окажется по центру)
- **url** - обязателен для типа *navigate*. Указывает URL, куда необходимо перейти.
- **selector** - обязателен для типов: *click*, *input*, *scrollTo*. Чистый XPath. Шаблон: `//тег[@аттрибут='значение']`. Параметров может быть несколько через `and`. Для поиска по тексту: `text()='Текст'`. Для частичного совпадения: `starts-with(@attr, 'начало_')`.
- **text** - обязателен для типа *input*. Указывает текст, вводимый в форму. Числовые значения автоматически приводятся к строке.
- **enter** - необязательный параметр для типа *input*. По умолчанию `true` — после ввода нажимается ENTER. Если `false`, ENTER не нажимается (полезно для форм с несколькими полями).
- **duration** - обязателен для типа *wait*. Количество секунд бездействия. Неотрицательное целое значение, `0` допустимо.
- **point** - обязателен для типов: *scrollUp*, *scrollDown*. Количество единиц перемещения (не пиксели). `0` допустимо.
- **behavior** - необязательный параметр для всех типов *scroll_*. Поведение скролла. Важно: `smooth` работает только со статическими страницами; на динамических `smooth` может не сработать корректно. Доступные значения:
    - *none (default)* - жёсткий переход без поведения
    - *smooth* - плавный скролл
- **wait** - длительность паузы **перед** выполнением действия в секундах. По умолчанию `1`. Используется, чтобы дать странице/элементу время дойти до нужного состояния.


# Особенности Screenshot Mode
## scene_object
> Содержит дополнительные необязательные поля *annotations*, *transition* и *transpeed*. Так же определяет обязательное поле *path*.

- \* **path** - путь до файла .png или .jpg, который будет использоваться в качестве основного изображения.
- **transition** - определяет тип перехода на следующий фрагмент. Доступные варианты:
    - *without* *(default)* - просто склейка
    - *slideRight* - скольжение вправо
    - *slideLeft* - скольжение влево
    - *slideUp* - скольжение вверх
    - *slideDown* - скольжение вниз
    - *blackout* - затемнение
- **transpeed** - задаёт значение в секундах, сколько будет длиться переход. Работает только в том случае, если значение перехода не является *without*.
- **annotations** - содержит *annotation_object*, которые будут размещены на фрагменте

#### annotation_object
> Определяет аннотацию на скриншоте по координатам. Началом координат выступает левый верхний угол. Название формируется по следующему шаблону: "annotation_id" - где id - уникальный порядковый номер. Все аннотации формируются по порядку, основанному на id.

- \* **type** - тип аннотации. От него зависит, какие параметры будут необходимы (кроме оставшихся обязательных). Доступные варианты:
    - *square* - подсвечивает область квадратом
    - *line* - строит линию
    - *arrow* - строит стрелку в одну сторону
    - *darrow* - строит стрелку в обе стороны
- \* **transparency** - определяет прозрачность аннотации. Значение от 0 до 1, где 0 - это отсутствие прозрачности, а 1 - отсутствие аннотации.
- \* **start_x** - доступен для аннотации любого типа. Задаёт изначальное положение аннотации по x.
- \* **start_y** - доступен для аннотации любого типа. Задаёт изначальное положение аннотации по y.
- \* **end_x** - доступен для аннотации любого типа. Задаёт конечное положение аннотации по x.
- \* **end_y** - доступен для аннотации любого типа. Задаёт конечное положение аннотации по y.
- **wait** - указывает длительность ожидания наложения аннотации в секундах. Если задано, аннотация появится только через переданный промежуток времени в рамках очереди, при этом задерживая оставшиеся изображения.
- **duration** - доступен для аннотации любого типа. Задает время действия аннотации в секундах. Если указано, то следующая аннотация появится только тогда, когда текущая будет окончена. 
- **label** - доступен для аннотации любого типа. Задаёт небольшой текст рядом с аннотацией в зависимости от её типа. По умолчанию отсутствует.
- **text** - доступен только для типов: *square*. Задаёт текст *внутри* аннотации.
### Дополнение: типы `line` и `darrow`
- `line` — прямая линия от `start_x/start_y` к `end_x/end_y`.
- `darrow` — двунаправленная стрелка (наконечники с обоих концов).

Пример `line`:
```yaml
annotation_1:
  type: line
  transparency: 0.0
  start_x: 200
  start_y: 150
  end_x: 800
  end_y: 400
```

Пример `darrow`:
```yaml
annotation_1:
  type: darrow
  transparency: 0.0
  start_x: 100
  start_y: 300
  end_x: 700
  end_y: 300
```

Важно:
- для `line` / `arrow` / `darrow` координаты **не нормализуются автоматически** (направление сохраняется);
- OCR (`ocr: true`) поддерживается только для `type: square`.

### Дополнение: `subplace` (позиция субтитров)
- `down` (default) — нижняя часть экрана;
- `up` — верхняя часть экрана;
- `center` — центральная область экрана.

Пример:
```yaml
scene_1:
  path: input/screen1.png
  duration: 5
  tts: "Текст субтитров в верхней части экрана"
  subtitles: true
  subplace: up
  subtitle_style: contrast
```

### Дополнение: синхронизация субтитров
- Субтитры стартуют одновременно с озвучкой (`start = 0` от начала сцены);
- длина субтитров автоматически ограничивается длиной аудио (допуск около ±200ms);
- если аудио длиннее сцены, субтитры обрезаются по длительности сцены.

### Дополнение: переходы `slide*`
Поддерживаются:
- `without`
- `blackout`
- `slideRight`
- `slideLeft`
- `slideUp`
- `slideDown`

`transpeed` — длительность перехода в секундах (default `1.0`, min `0.1`).

Пример:
```yaml
scene_1:
  path: input/screen1.png
  duration: 5
  transition: slideRight
  transpeed: 0.8
scene_2:
  path: input/screen2.png
  duration: 4
```

### Дополнение: checkpointing
После каждой успешно обработанной сцены screenshot-движок сохраняет checkpoint:
- `output/.{title}_checkpoint.json`

Временный кэш сцен:
- `output/.scene_cache/scene_<N>.mp4`

При повторном запуске той же команды рендер продолжается с последней успешно обработанной сцены.
После успешного завершения:
- checkpoint удаляется автоматически;
- кэш сцен удаляется, если `metadata.save_files: false`.

---

# script format specification

документ описывает актуальный yaml-формат сценария для screenshot mode.

## 1. корневая структура
```yaml
metadata: {}
acts:
  act_1:
    scenes:
      scene_1: {}
```

- `metadata` — параметры итогового ролика.
- `acts` — логические блоки.
- `scene_*` — конкретные сцены в порядке id.

## 2. metadata
```yaml
metadata:
  title: demo
  resolution: 1920x1080
  mode: screenshot
  fps: 24
  language: ru
  description: demo video
```

обязательные:
- `title`
- `resolution`

рекомендуемые:
- `mode: screenshot`
- `fps` (24/30/60)
- `language`

## 3. scene object (screenshot)
```yaml
scene_1:
  name: main screen
  path: input/screen1.png
  duration: 4
  tts: "это стартовая страница"
  voice: zahar
  subtitles: true
  subtitle_style: classic
  subtitle_font_size: 40
  subtitle_max_chars: 90
  subtitle_bg_opacity: 0.55
  effect: without
  transition: blackout
  transpeed: 0.8
```

### обязательные поля
- `path` — путь к png/jpg в screenshot mode.

### важные поля
- `duration` — минимальная длительность сцены.
- `tts` — текст озвучки.
- `subtitles` — включение субтитров.

### стили субтитров
`subtitle_style`:
- `classic`
- `minimal`
- `contrast`
- `cinematic`

## 4. text object
```yaml
texts:
  text_1:
    text: "краткая инструкция"
    start_x: 120
    start_y: 120
    size: 42
    wait: 0.2
    duration: 3.5
```

обязательные:
- `text`
- `start_x`, `start_y`
- `size`

## 5. annotation object
поддержаны типы:
- `square`
- `arrow`

### 5.1 ручные координаты
```yaml
annotations:
  annotation_1:
    type: square
    transparency: 0.25
    start_x: 130
    start_y: 90
    end_x: 630
    end_y: 310
```

### 5.2 smart annotation (без ручных координат)
```yaml
annotations:
  annotation_1:
    type: square
    transparency: 0.22
    target_text: "Templates"
    target_index: 0
    auto_padding: 16
    auto_expand_width: 10
    auto_expand_height: 8
```

поля smart-режима:
- `target_text` — ключевая фраза на скриншоте.
- `target_index` — выбор совпадения при дубликатах.
- `auto_padding`, `auto_expand_width`, `auto_expand_height` — расширение bbox.
- `auto_from` — направление старта стрелки для типа `arrow`.

## 6. OCR параметры аннотации
```yaml
annotations:
  annotation_1:
    type: square
    transparency: 0.25
    target_text: "Templates"
    ocr: true
    ocr_lang: rus+eng
    ocr_min_conf: 0.35
    ocr_target: both
```

- `ocr` — включить OCR внутри ROI.
- `ocr_lang` — языки tesseract (`rus+eng`).
- `ocr_min_conf` — порог confidence `0..1`.
- `ocr_target`:
  - `overlay`
  - `metadata`
  - `both`

## 7. полный пример screenshot mode
```yaml
metadata:
  title: screenshot_demo
  resolution: 1920x1080
  mode: screenshot
  fps: 24

acts:
  act_1:
    name: intro
    scenes:
      scene_1:
        name: templates tab
        path: input/screen1.png
        duration: 5
        tts: "переходим в раздел templates"
        subtitles: true
        subtitle_style: contrast
        annotations:
          annotation_1:
            type: square
            transparency: 0.24
            target_text: "Templates"
            auto_padding: 18
            ocr: true
            ocr_target: both
          annotation_2:
            type: arrow
            transparency: 0.05
            target_text: "Create"
            auto_from: left
```

## 8. типовые ошибки
1. `path` отсутствует в screenshot-сцене.
2. неверный формат `resolution`.
3. слишком высокий `ocr_min_conf` (например `0.95`) на низкоконтрастных скринах.
4. `target_text` не находится в кадре из-за другого языка/региcтра/качества.
5. слишком длинные субтитры без ограничения `subtitle_max_chars`.
